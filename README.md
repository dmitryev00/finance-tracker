<p align="center">
    <img src="assets/finance-header.svg" alt="Finance Tracker" /> -->
</p>

# 💰 Finance Tracker

REST API на Spring Boot для управления личными финансами — пользователи, кошельки, транзакции и баланс.

![Java](https://img.shields.io/badge/Java-21-orange)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3-green)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-DB-lightblue)
![Maven](https://img.shields.io/badge/Maven-build-blue)

---

## 🚀 Описание

**Finance Tracker** — backend-сервис для учёта финансов:

- управление пользователями
- несколько кошельков на одного пользователя
- добавление/удаление транзакций
- автоматическое обновление баланса кошелька
- генерация `transferId` для транзакций
- глобальная обработка ошибок через `@ControllerAdvice`

---

## 🧠 Архитектура

Проект построен по многослойной архитектуре:

| Слой       | Функция |
|------------|---------|
| Controller | Обрабатывает HTTP-запросы |
| Service    | Бизнес-логика, проверка баланса, генерация `transferId` |
| Repository | Работа с PostgreSQL через Spring Data JPA |
| DTO        | Модели для ввода/вывода данных (`Input` и `Response`) |
| Mapper     | Преобразование Entity ↔ DTO |
| Exception  | Глобальная обработка ошибок |

---

## 📦 Основные сущности

- **UserEntity** — пользователь  
- **WalletEntity** — кошелёк с балансом  
- **TransactionEntity** — транзакция с `transferId` и `createdAt`  

---

## 🗂 DTO

- `UserInput`, `UserResponse` — модели пользователя  
- `WalletInput`, `WalletResponse` — модели кошелька  
- `TransactionInput`, `TransactionResponse` — модели транзакции  
- Мапперы: `UserMapper`, `WalletMapper`, `TransactionMapper`  

---

## ⚡ Особенности

- Генерация уникального `transferId` через `TransferIdGenerator`
- Проверка достаточности средств перед списанием
- Баланс кошелька автоматически обновляется при каждой транзакции
- Глобальная обработка ошибок через `GlobalExceptionHandler`
